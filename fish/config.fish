alias list 'ls -lA'

alias up 'cd ..'
alias back 'cd -'
alias down 'cd (git rev-parse --show-toplevel)'

alias dotfiles 'cd ~/dotfiles'

alias rm 'echo "rm is disabled! Use trash or remove."'
alias remove '/bin/rm -irv'
alias cp 'cp -i'
alias mv 'mv -i'
