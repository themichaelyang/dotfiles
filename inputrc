# set up vim keybindings!
set editing-mode vi

$if mode=vi
  set keymap vi-command
  "gg": beginning-of-history
  "G": end-of-history
  set keymap vi-insert
  "jk": vi-movement-mode
$endif

# single tab for suggestions
set show-all-if-ambiguous on
